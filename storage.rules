rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Phase 8: tighten per-path once file upload features (e.g. challan
    // photos, deposit proof images) are designed.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
